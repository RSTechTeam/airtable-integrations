name: Main Workflow
on:
  workflow_dispatch:
  schedule:
    - cron: '0/10 * * * *'
jobs:
  accounting_sync:
    uses: ./.github/workflows/base.yml
    with:
      filename: 'accounting_sync'
      use-accounting-base: true
    secrets: inherit
